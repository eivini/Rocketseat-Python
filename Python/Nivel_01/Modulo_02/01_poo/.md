<div>
    <h1>
        Programação Orientada a Objetos <b>(POO)</b>
    </h1>
    <p>
        A Programação Orientada a Objetos (POO) é um paradigma de programação baseado no conceito de "objetos". Objetos são instâncias de classes, que podem conter atributos (dados) e métodos (funções que realizam operações com os dados). A POO visa tornar o código mais modular, reutilizável e mais fácil de entender e manter.
    </p>
</div>

<div>
    <h1>
        Os 4 pilares da POO:
    </h1>
    <h3>
        Abstração:
    </h3>
    <p>
        A abstração é o processo de esconder os detalhes complexos e mostrar apenas o necessário. Em Python, você pode usar classes para criar uma abstração do mundo real.
        <li><b>Exemplo</b>:
        <br>
        uma classe Carro pode ter atributos como modelo, cor e ano, mas o funcionamento interno (como o motor) pode ser oculto, oferecendo apenas métodos para acelerar ou frear.
    </p>
</div>

```Python
class Carro:
    def __init__(self, modelo, cor, ano):
        self.modelo = modelo
        self.cor = cor
        self.ano = ano
    
    def acelerar(self):
        print("O carro acelerou!")
    
    def frear(self):
        print("O carro freou!")
```

<div>
    <h3>
        Encapsulamento:
    </h3>
    <p>
        O encapsulamento refere-se à prática de esconder o estado interno de um objeto e fornecer uma interface pública para interagir com ele.
        <li><b>Exemplo</b>:
        <br>
        Em Python, você pode usar métodos getter e setter para acessar e modificar atributos privados (embora não haja modificadores de acesso rígidos como em outras linguagens, como Java, o padrão é usar convenções de nomes com underline, como self._atributo).
    </p>
</div>

```Python
class Carro:
    def __init__(self, modelo, cor, ano):
        self._modelo = modelo
        self._cor = cor
        self._ano = ano
    
    def get_modelo(self):
        return self._modelo
    
    def set_modelo(self, modelo):
        self._modelo = modelo
```

<div>
    <h3>
        Herança:
    </h3>
    <p>
        A herança permite que uma classe herde atributos e métodos de outra, promovendo a reutilização de código.
        <li><b>Exemplo</b>:
        <br>
        Em Python, uma classe pode herdar de outra simplesmente colocando o nome da classe base entre parênteses ao definir a nova classe.
    </p>
</div>

```Python
class Veiculo:
    def __init__(self, modelo):
        self.modelo = modelo
    
    def ligar(self):
        print("O veículo ligou!")

class Carro(Veiculo):
    def __init__(self, modelo, cor):
        super().__init__(modelo)
        self.cor = cor
    
    def buzinar(self):
        print("Buzinando!")
```

<div>
    <h3>
        Polimorfismo:
    </h3>
    <p>
        O polimorfismo permite que diferentes classes implementem métodos com o mesmo nome, mas com comportamentos diferentes. 
        <li><b>Exemplo</b>:
        <br>
        Em Python, isso pode ser feito por meio de sobrescrita de métodos.
    </p>
</div>

```Python
class Animal:
    def fazer_som(self):
        print("O animal faz um som.")

class Cachorro(Animal):
    def fazer_som(self):
        print("O cachorro late.")

class Gato(Animal):
    def fazer_som(self):
        print("O gato mia.")
```

<div>
    <h3>
        Testando o polimorfismo
    </h3>
</div>

```Python
animais = [Cachorro(), Gato()]
for animal in animais:
    animal.fazer_som()  # Cada um faz um som diferente
```

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
<div>
    <h1>
        Programação Orientada a Objetos <b>(POO)</b>
    </h1>
    <p>
        A Programação Orientada a Objetos (POO) é um paradigma de programação que utiliza o conceito de "objetos". Objetos são instâncias de classes, podendo conter atributos (dados) e métodos (funções que operam sobre esses dados). A POO busca tornar o código mais modular, reutilizável, e fácil de entender e manter.
    </p>
</div>

<div>
    <h1>
        Os 4 pilares da POO:
    </h1>
    <h3>
        Abstração:
    </h3>
    <p>
        A abstração é o processo de esconder os detalhes complexos e mostrar apenas o necessário, focando nas funcionalidades principais. Em Python, podemos criar abstrações usando classes para representar entidades do mundo real.
    </p>
    <ul>
        <li><b>Exemplo</b>: 
            <br>
            Uma classe `Carro` pode ter atributos como modelo, cor e ano. O funcionamento interno (como o motor) pode ser oculto, oferecendo apenas métodos públicos para acelerar ou frear o veículo.
        </li>
    </ul>
</div>

```Python
class Carro:
    def __init__(self, modelo, cor, ano):
        self.modelo = modelo
        self.cor = cor
        self.ano = ano
    
    def acelerar(self):
        print("O carro acelerou!")
    
    def frear(self):
        print("O carro freou!")
```

<div>
    <p><b>Saída Esperada:</b></p>
</div>

```txt
O carro acelerou!
O carro freou!
```

<div>
    <h3>
        Encapsulamento:
    </h3>
    <p>
        O encapsulamento envolve ocultar o estado interno de um objeto e fornecer uma interface pública para interagir com ele, protegendo dados sensíveis.
    </p>
    <ul>
        <li><b>Exemplo</b>: 
            <br>
            Em Python, podemos usar métodos getter e setter para acessar e modificar atributos privados. A convenção é prefixar atributos privados com um único underscore (`_`).
        </li>
    </ul>
</div>

```Python
class Carro:
    def __init__(self, modelo, cor, ano):
        self._modelo = modelo
        self._cor = cor
        self._ano = ano
    
    def get_modelo(self):
        return self._modelo
    
    def set_modelo(self, modelo):
        self._modelo = modelo
```

<div>
    <p><b>Saída Esperada:</b></p>
</div>

```txt
Modelo do carro: Ferrari
```

<div>
    <h3>
        Herança:
    </h3>
    <p>
        A herança permite que uma classe herde atributos e métodos de outra, facilitando a reutilização de código e a criação de novas funcionalidades.
    </p>
    <ul>
        <li><b>Exemplo</b>: 
            <br>
            Em Python, uma classe pode herdar de outra colocando o nome da classe base entre parênteses na definição da nova classe.
        </li>
    </ul>

    ```Python
    class Veiculo:
        def __init__(self, modelo):
            self.modelo = modelo
        
        def ligar(self):
            print("O veículo ligou!")

    class Carro(Veiculo):
        def __init__(self, modelo, cor):
            super().__init__(modelo)
            self.cor = cor
        
        def buzinar(self):
            print("Buzinando!")
    ```

    <p><b>Saída Esperada:</b></p>
    ```txt
    O veículo ligou!
    Buzinando!
    ```

    <h3>
        Polimorfismo:
    </h3>
    <p>
        O polimorfismo permite que diferentes classes implementem métodos com o mesmo nome, mas com comportamentos diferentes, promovendo flexibilidade no código.
    </p>
    <ul>
        <li><b>Exemplo</b>: 
            <br>
            Em Python, isso pode ser feito por meio de sobrescrita de métodos, onde as subclasses alteram o comportamento de um método da classe base.
        </li>
    </ul>

    ```Python
    class Animal:
        def fazer_som(self):
            print("O animal faz um som.")

    class Cachorro(Animal):
        def fazer_som(self):
            print("O cachorro late.")

    class Gato(Animal):
        def fazer_som(self):
            print("O gato mia.")
    ```

    <h3>
        Testando o Polimorfismo:
    </h3>
    <p>
        Aqui, mostramos como diferentes objetos podem ter comportamentos diferentes, mesmo quando utilizam o mesmo método `fazer_som`.
    </p>

    ```Python
    animais = [Cachorro(), Gato()]
    for animal in animais:
        animal.fazer_som()  # Cada um faz um som diferente
    ```

    <p><b>Saída Esperada:</b></p>
    ```txt
    O cachorro late.
    O gato mia.
    ```

</div>


Programação Orientada a Objetos (POO)
A Programação Orientada a Objetos (POO) é um paradigma de programação baseado no conceito de "objetos". Objetos são instâncias de classes, que podem conter atributos (dados) e métodos (funções que realizam operações com os dados). A POO visa tornar o código mais modular, reutilizável e mais fácil de entender e manter.

Os 4 pilares da POO:
Abstração

A abstração é o processo de esconder os detalhes complexos e mostrar apenas o necessário. Em Python, você pode usar classes para criar uma abstração do mundo real.

Exemplo: uma classe Carro pode ter atributos como modelo, cor e ano, mas o funcionamento interno (como o motor) pode ser oculto, oferecendo apenas métodos para acelerar ou frear.

python
Copiar
class Carro:
    def __init__(self, modelo, cor, ano):
        self.modelo = modelo
        self.cor = cor
        self.ano = ano
    
    def acelerar(self):
        print("O carro acelerou!")
    
    def frear(self):
        print("O carro freou!")
Encapsulamento

O encapsulamento refere-se à prática de esconder o estado interno de um objeto e fornecer uma interface pública para interagir com ele.

Em Python, você pode usar métodos getter e setter para acessar e modificar atributos privados (embora não haja modificadores de acesso rígidos como em outras linguagens, como Java, o padrão é usar convenções de nomes com underline, como self._atributo).

python
Copiar
class Carro:
    def __init__(self, modelo, cor, ano):
        self._modelo = modelo
        self._cor = cor
        self._ano = ano
    
    def get_modelo(self):
        return self._modelo
    
    def set_modelo(self, modelo):
        self._modelo = modelo
Herança

A herança permite que uma classe herde atributos e métodos de outra, promovendo a reutilização de código.

Em Python, uma classe pode herdar de outra simplesmente colocando o nome da classe base entre parênteses ao definir a nova classe.

python
Copiar
class Veiculo:
    def __init__(self, modelo):
        self.modelo = modelo
    
    def ligar(self):
        print("O veículo ligou!")

class Carro(Veiculo):
    def __init__(self, modelo, cor):
        super().__init__(modelo)
        self.cor = cor
    
    def buzinar(self):
        print("Buzinando!")
Polimorfismo

O polimorfismo permite que diferentes classes implementem métodos com o mesmo nome, mas com comportamentos diferentes. Em Python, isso pode ser feito por meio de sobrescrita de métodos.

python
Copiar
class Animal:
    def fazer_som(self):
        print("O animal faz um som.")

class Cachorro(Animal):
    def fazer_som(self):
        print("O cachorro late.")

class Gato(Animal):
    def fazer_som(self):
        print("O gato mia.")

# Testando o polimorfismo
animais = [Cachorro(), Gato()]
for animal in animais:
    animal.fazer_som()  # Cada um faz um som diferente