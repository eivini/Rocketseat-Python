<div align="flex-start" title="for">
    <h1>
        01 - for: 
    </h1>
    <p>
        O laço for é usado para iterar sobre uma sequência (como uma lista, tupla, string ou intervalo de números). Ele repete um bloco de código para cada item na sequência.
    </p>
    <h3>
        Exemplo:
    </h3>

```python
for i in range(5):  # Itera de 0 a 4
    print(i)

```

<p>
    Neste exemplo, o `for` vai imprimir os números de 0 a 4.
</p>
</div>

<div align="flex-start" title="">
    <h1>
        
    </h1>
    <p>
        
    </p>
    <h3>
        Exemplo:
    </h3>

```python

```

</div>

##  01 - for: 
    O laço for é usado para iterar sobre uma sequência (como uma lista, tupla, string ou intervalo de números). Ele repete um bloco de código para cada item na sequência.

    Exemplo:

    for i in range(5):  # Itera de 0 a 4
        print(i)

    Neste exemplo, o for vai imprimir os números de 0 a 4.

##  02 - while: 
    O laço while repete um bloco de código enquanto uma condição for verdadeira. Ele continua executando até que a condição se torne falsa.

    Exemplo:

    i = 0
    while i < 5:  # Repete enquanto i for menor que 5
        print(i)
        i += 1
        
    Neste caso, o while vai imprimir os números de 0 a 4, e a execução termina quando a condição i < 5 não for mais verdadeira.